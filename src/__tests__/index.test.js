const { files } = require("../index");
// ratatoskr:exclude
describe("index", () => {
    it("should load files and display them in the file list", () => {
        // Add your test logic here
    });
});